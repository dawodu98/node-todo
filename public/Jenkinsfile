pipeline {
    agent any

    stages {
                stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }    


        stage('Build Angular Project') {
            steps {
                sh 'npm run build'
            }
        }


        
    }
}
